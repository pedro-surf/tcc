overwrite: true
schema: "http://localhost:3000/graphql"
documents:
  - "src/**/*.graphql"        # raw .graphql files
  - "src/graphql/**/*.ts" 
generates:
  src/generated/graphql.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-query
    config:
      fetcher: fetch
      exposeQueryKeys: true
      skipTypename: false
      preResolveTypes: true